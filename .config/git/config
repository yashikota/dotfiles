[user]
    name = yashikota
    email = 52403688+yashikota@users.noreply.github.com
    signingKey = ~/.ssh/id_ed25519.pub
[core]
    editor = vim
    pager = delta
    excludesfile = ~/.config/git/ignore
[fetch]
    prune = true
[init]
    defaultBranch = main
[pull]
    rebase = true
[gpg]
    format = ssh
[commit]
    gpgsign = true
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection
    side-by-side = true
    line-numbers = true
[merge]
    conflictstyle = zdiff3
[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta
[alias]
    default = "!f(){ b=$(git symbolic-ref --quiet --short refs/remotes/origin/HEAD 2>/dev/null | sed 's#^origin/##'); if [ -z \"$b\" ]; then b=$(git remote show origin 2>/dev/null | sed -n 's/.*HEAD branch: //p' | head -n1); fi; if [ -z \"$b\" ]; then for c in main master; do if git show-ref --verify --quiet \"refs/heads/$c\" || git show-ref --verify --quiet \"refs/remotes/origin/$c\"; then b=$c; break; fi; done; fi; [ -n \"$b\" ] || { echo \"Could not determine default branch\" >&2; exit 1; }; git switch -q \"$b\"; }; f"
